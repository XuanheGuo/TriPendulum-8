defaults: configs/default.yaml
algorithm:
  name: SAC
  total_timesteps: 300000
  learning_rate: 0.0003
  buffer_size: 300000
  batch_size: 128
  gamma: 0.99
  tau: 0.005
  train_freq: 2
  gradient_steps: 2
  learning_starts: 5000
  resume_learning_starts: 5000
  ent_coef: auto_0.2
  target_entropy: -1.0
  action_noise_sigma: 2.0
  policy: MlpPolicy
  save_freq: 100000
  save_replay_buffer: false
