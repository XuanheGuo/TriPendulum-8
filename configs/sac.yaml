defaults: configs/default.yaml
algorithm:
  name: SAC
  total_timesteps: 300000
  learning_rate: 0.0003
  buffer_size: 500000
  batch_size: 256
  gamma: 0.99
  tau: 0.005
  train_freq: 1
  gradient_steps: 1
  learning_starts: 5000
  ent_coef: auto
  policy: MlpPolicy
  save_freq: 25000
